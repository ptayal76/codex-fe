import React from 'react';
import './Heading.css'; // Import the CSS file for styling

const Heading = () => {
    return (
        <div className="heading">
            <h1>ThoughtSpot Diagnostic Portal</h1>
        </div>
    );
};

export default Heading;
